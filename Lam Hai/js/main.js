/*$(document).ready(function(){
    var width  = $(window).width();
    var temp = (20/100) * width;
    temp += 'px';
	$('.slider').slick({
	 centerMode: true,
	 cssEase: 'linear',    
	 autoplay: true,
	 prevArrow: '.left',
	 nextArrow: '.right',
	 centerPadding: temp,
	 slidesToShow: 1,
	});
});*/